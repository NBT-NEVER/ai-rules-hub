interface:
  display_name: "Skill Maintainer"
  short_description: "维护自定义 skill 体系，处理反思写回，并将 src 自动同步到 Codex 与 Cursor。"
  default_prompt: "Use $skill-maintainer to create, update, rename, install, synchronize, auto-deploy shared custom skills from src, and write new durable lessons back into $recall-thinking."
