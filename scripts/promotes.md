修改I:\PYTHON\OWN_PROJECT\backup-test-cursor\ai-rules-hub\scripts下面的代码。总要求：在powershell执行完不要自己关闭窗口，完成则输出“已完成，任意键关闭窗口”失败时用户需要看到报错信息，不要自动关闭窗口；I:\PYTHON\OWN_PROJECT\backup-test-cursor\ai-rules-hub\scripts\core不要动，core作为你要写的代码的调用，调用时使用相对路径。1.需要完成pull-and-deploy.ps1。完成功能：a.从github此项目中拉取项目，到本项目地址，如果pull-and-deploy.ps1脚本上面没有ai-rules-hub\scripts父文件夹，则询问本地项目地址，让用户输入本地地址；因为以后使用的很多电脑不同，所以项目地址也不同，所以写成相对地址.b.如果远端项目版本超前，就直接覆盖本地，如果本地有未提交的版本，则询问：覆盖或者停止程序，并实现选择。c.在拉取前，自动检测本电脑的代理形式和接口，根据此修改git代理，提高拉去成功率 d.留有调用接口，在后续程序中可以调用e.拉去之后调用update-from-src.ps1，完成本地的部署   2.需要github-push.ps1 。完成功能：a.将本项目暂存、提交、推送到远端，远端地址写死在程序内，本地项目地址因为以后使用的很多电脑不同，所以项目地址也不同，所以写成相对地址b.在拉取前，自动检测本电脑的代理形式和接口，根据此修改git代理，提高拉去成功率c.弹出提交时的说明，让用户输入说明，回车完成提交和推送。    3.完成